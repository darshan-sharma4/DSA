## Functions
-->Functions are the group of code which takes some input and perform some operations to provide a meaningful output.

Function in memory
-->Functions are generally store in stack memory, also when the work of a particular function is returned then it get clear from the stack memory and the variable associated with that function also get clear from the memory. 

**Pass by value**
-->Pass by vlaue means we are passing the copy value of some data to some other function if some  change occurs with that data they does not reflect to the original value.

