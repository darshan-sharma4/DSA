Data structure is way to store data  in memory and algorithm are used to retrieve that data in efficient way from the storage.
## Arrary
-->Array are used to store same type of data in a contiguous manner. It's a linear data structure.

--> Array always used pass by reference means actual address of the variable is passed.

# Pass by refernce
-->In pass by reference actual address is sent of the varialble so, when any change happens it directly change over the original address.

# Linear search
Linear search is one of most easiest searhing algorithm to traverse over array.

Q. Find the smallest and greatest number in an array.
Q. Find the index also.
Q. wAF to calculate sum and product of all numbers in an array    
Q.WAF to swap the max and min number of an array.    
Q. WAF to print all the unique values in an array.
WAF to print intersection of 2 arrays.


